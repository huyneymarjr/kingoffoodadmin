import type { ThemeConfig } from 'antd';

const theme: ThemeConfig = {
  token: {
    fontSize: 14,
    colorPrimary: '#217ca3',
    colorBorder: '#8fd3f0',
    colorLink: '#217ca3',
  },

  components: {
    Button: {
      borderRadius: 10,
    },
  },
};

//#64a4bf màu background menu
export default theme;
