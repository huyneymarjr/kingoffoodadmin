import TableCustom from './TableCustom';

const PreOrders = ({ startDate, endDate }: any) => {
  return <TableCustom type={4} startDate={startDate} endDate={endDate} />;
};

export default PreOrders;
