import TableCustom from './TableCustom';

const PaindOrders = ({ startDate, endDate }: any) => {
  return <TableCustom type={1} startDate={startDate} endDate={endDate} />;
};

export default PaindOrders;
